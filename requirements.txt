Flask>=2.3.0
flask-cors>=4.0.0
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0
requests>=2.31.0
python-dotenv>=1.0.0
PyMySQL>=1.1.0
cryptography>=42.0.0

# ---- 自适应 Agentic RAG（可选，缺失时自动降级为关键词/哈希向量检索）----
# sentence-transformers>=2.2.0   # 语义向量 embedding（含 torch，体积较大）
# pydantic>=2.0.0                # 结构化输出（Router/Reflector）